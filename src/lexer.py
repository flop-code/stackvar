def stv_lexer(parsed_code: dict) -> bool:
    """
    Stackvar lexer.
    Checks correctness of Stackvar code.
    Accepts a parsed dictionary (parsed_code) and returns a boolean (0 if no errors, 1 if else).
    Also returns 0 if there are warnings.
    Prints all errors and warnings.
    Look in ../docs.txt for more information.
    """
    pass  # todo: implement lexer
